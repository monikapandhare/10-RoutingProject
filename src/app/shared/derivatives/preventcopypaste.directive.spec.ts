import { PreventcopypasteDirective } from './preventcopypaste.directive';

describe('PreventcopypasteDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventcopypasteDirective();
    expect(directive).toBeTruthy();
  });
});
